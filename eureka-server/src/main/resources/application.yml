spring:
  application:
    name: eureka-server
server:
  port: 8111
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl.defaultZone: http://localhost:8111/eureka/