spring:
  application:
    name: pubUser-service
  jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/shop
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver

server:
  port: 8220

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8111/eureka/


